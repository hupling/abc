name: build and test Backend

on: [push]

jobs:
  
  compliance:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

  produce-artifacts:
    runs-on: ubuntu-latest

    steps:

      - name: a
        run: |

          echo ${{ fromJSON(github) }}
